import { HighlightQuote.DirectiveDirective } from './highlight-quote.directive.directive';

describe('HighlightQuote.DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightQuote.DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
